package com.personal.leaguehistory.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * Ported from the web record book's CSS custom properties so the app and the
 * site read as the same product. Names match the CSS variables where possible.
 */

val Navy = Color(0xFF0A1626)
val NavyDeep = Color(0xFF071827)
val NavyLift = Color(0xFF123252)
val Ink = Color(0xFF0D1F33)
val Muted = Color(0xFF5F7086)
val LineLight = Color(0xFFE2E7EE)
val SurfaceLight = Color(0xFFF5F7FA)
val PanelLight = Color(0xFFFFFFFF)

val LeagueBlue = Color(0xFF1A63E6)
val LeagueBlueDeep = Color(0xFF0D4FBD)
val LeagueBlueSoft = Color(0xFFD8E5FF)

val Gold = Color(0xFFF2A91F)
val GoldSoft = Color(0xFFFFF2CF)
val GoldDeep = Color(0xFF8A5D00)

val LeagueRed = Color(0xFFDC2F2F)
val LeagueRedSoft = Color(0xFFFFE0E0)
val LeagueGreen = Color(0xFF128A53)

/* Dark-mode surfaces. The web build has no dark theme, so these are derived
   from the navy hero gradient rather than ported one-for-one. */
val DarkSurface = Color(0xFF0B1420)
val DarkSurfaceContainer = Color(0xFF14202F)
val DarkSurfaceContainerHigh = Color(0xFF1B2A3C)
val DarkLine = Color(0xFF27384C)
val DarkOnSurface = Color(0xFFE6EDF5)
val DarkMuted = Color(0xFF9AAABE)

val BlueLightOnDark = Color(0xFF8FB6FF)
val GoldLightOnDark = Color(0xFFFFC65C)
val RedLightOnDark = Color(0xFFFF8A8A)
val GreenLightOnDark = Color(0xFF5FD3A0)
